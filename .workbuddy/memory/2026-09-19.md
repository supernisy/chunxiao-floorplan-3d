# 2026-09-19 工作日志

## 任务：平面图(+视频/CAD) → 可交互 3D 毛坯房（并萃取 floorplan-to-3d skill）
工作区：`C:\Users\super\WorkBuddy\2026-09-19-02-22-25`

### 素材
- `assets/chunxiao.dwg`（5.5MB，未用，本机无 DWG→DXF 工具）
- `assets/chunxiao.pdf`（282KB，**主源**，CAD 导出且保留 OCG 图层语义）
- `assets/walkthrough.mp4`（36MB，经 资料库 skill 拉取，未用于校准）

### 关键突破
1. **PDF 保留 OCG 图层**（`P-WALL`/`P-DOOR`/`COMM-GLAZ-SECT`/`FLOR-STAIR`…）→ 无需 DWG 也拿到图层语义。
2. PDF 文字是**矢量填充轮廓**（`get_text` 返回 0 条）→ 房间名/尺寸只能靠渲染后视觉读取。
3. 房间分割唯一可行路径：**从 layers.json 按图层合成单层 mask**，而非彩色渲染 + HSV 阈值（后者彻底失败）。
4. 门解析：`P-DOOR` 橙色 poly = 门扇、灰色 = 开启弧；门洞另一端用**弧点最远点**判定。
5. 房间分割最终法：`close(wall∪glass,61) ∪ dilate(door_sym,15) ∪ 门洞线 ∪ 门扇线` + watershed 归还凹角。
6. 门禁通过：16 房间 / 19 门 / 可达 14（不可达的是 2 个飘窗，正确）/ 143.03 m²。

### 当前状态
- 第 0–5 层完成，第 6 层「观感校验」部分完成（俯视图整体正确）。
- **遗留：黑色锐利三角碎片**（左上厨房区最明显）。已排除几何自交、阴影相机范围；疑似 `ExtrudeGeometry` 多环 earcut 在墙交叉处失败。
- 待验证修法：shapely 拆无洞简单多边形分别挤出，或三角化后直传 BufferGeometry。
- 详细踩坑与证据见 `RUN-LOG.md`。
- `floorplan-to-3d` skill **尚未开始**（须等成品验证通过）。

### 环境经验
- 本机 Bash 工具 PATH 异常（缺 `head`/`dirname`），文件盘点优先用 PowerShell 并**写入文件再 Read**（stdout 捕获不稳）。
- 截图走 headless Edge + CDP（`tools/shot.mjs`），端口 9333，独立 profile `~/.cdp_shot_profile`。
- vite dev server：`npm run dev` → `127.0.0.1:5173`。JSON 数据变更不会被 HMR 可靠热更，截图前需新开 tab。
- PATH 修复：Bash 工具缺 `ls`/`dirname`/`sleep`/`tail` → 每条命令前 `export PATH="/c/Users/super/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:$PATH"`（Bash 工具无持久 shell 状态）。
- 新增 `tools/capture.sh`：一条命令内自管理「起 vite(:5199) → 起 headless Edge(CDP:9333) → 截图 → 精确清理（按调试端口匹配杀 msedge，不误伤用户浏览器）」，解决跨 Bash 命令进程会死的问题。

---

## 第 7 轮（黑三角根除 + 房间不规则正本清源）

### 救数据
- `data/room-graph.json` 被 12:09 的 `rooms_v4.py` 重跑冲成 **1 房间**（原 16）→ `keep_area` 仅 11.8㎡ → 15 条墙带被裁光。
- 从 `data/_bak/`（11:52 备份）恢复 16 房间/19 门。坏版存为 `*.broken_1209.json`。

### 三条根因（都已修）
1. **CDT 长宽比过滤误杀墙主体**：墙是 3m×0.2m 细长条，CDT 三角形天然 ~15:1，被「高/底<0.10」判据当退化三角删掉 → 只剩边缘碎片。**修正**：取消长宽比过滤，只剔近零面积三角。
2. **PDF 墙线本身微斜**（矢量层实测：1–5° 占 11%、>5° 占 7%，多为 `COLUWL-HATCH` 柱墙填充斜边，带固定 8.4pt 偏移）→ 这才是「房间不规则」真根因。
3. **裁剪基准 `keep_area` 有 45° 切角**（mitre buffer 在锐角处）+ `clean_geom` 的形态学 opening 在凹角产生 45° 切角。

### 三个关键修法
- **轴向吸附用并查集**（不是迭代）：「水平边」→ `y_i=y_j`、「垂直边」→ `x_i=x_j` 约束，归组取均值。迭代版在 132 点大环上震荡（17.8%→39.4% 并扭出畸形）。
- **ortho 放裁剪之后、逐子多边形做**：放裁剪前等于白做（裁剪又引入新斜边）。
- **公摊处理撤回「difference 抠除」回到 `intersection(keep_area)` 裁剪**：抠除只扩 2cm 盖不住公摊的墙，残留成「一圈墙+爪印洞」。

### 结果
- 墙带轴线率 **58.7% → 93.7%**，房间 91.6%；黑三角绝迹；公摊清空；12 房间/11 门。
- 前端 `WallBand` 改为顶面用 CDT 三角网格 + 侧壁轮廓边拉伸（彻底不走 earcut）。
- 新增 `?mode=walk` 便于截图/直链漫游。
- 截图产物：`data/shot_top.png`、`shot_wall.png`、`shot_walk.png`。

### 遗留
- 飘窗区（y≈1.6–4.7, x≈10.1–12.7）残留锯齿状斜边（栏杆/玻璃剖线被误提取为墙），局部次要。
- `rooms_v4.py` 房间分割 bug 未修（靠备份绕过，根因待查）。

---

## 第 8 轮（`geom_v6.py` 全新管线 · 用户四条验收全达成）

用户诉求：①墙体不出现 90° 以外角；②厨房旁公卫形状明显不对；③按 PDF 图例正确理解门窗；④厨房阳台是**玻璃移门**。
产线文件：`pipeline/geom_v6.py`（新，637 行）→ 输出 `app/src/data/{walls_poly,room-graph,roof_poly}.json`。

### 本轮四个新根因（全部已修，且都是"斜边/假门"类问题的真凶）
1. **`triangulate_walls.py` 被零长 segment 打崩**：v6 的 `walls_poly.json` 每个环末尾复写首点（闭环冗余），Shewchuk CDT 直接 **segfault（exit 139，无任何输出）**。
   → 修：入参前 `clean_ring()` 去相邻重复点 + 去首尾重复点；shell <3 点跳过。
   ⚠️ 症状识别：python 进程 exit 139 且 stdout 全空 → 先怀疑 CDT 输入有退化环。
2. **`CHAIN_APPROX_SIMPLE` 把 45° 像素阶梯压成 45° 斜边**（「房间有斜墙」的矢量层根因，掩膜本身是正交的）。
   → 修（`geom_v5.mask_to_polys`）：改用 `CHAIN_APPROX_NONE` + 新增 `_ortho_ring()` 把对角步 L 形展开 + 3×3 开闭净化。房间边数 1020→368。
3. **`build_envelope` 用方形核 `np.ones((k,k))` 做形态学闭合**：凹角被沿对角线磨出 45° 阶梯（k=0.9m 时长达 0.9m），findContours 再压成一条大斜边，房间掩膜 `(barrier==0)&(env>0)` 继承之。
   → 修：新增 `_axis_close(m,k)`＝水平闭合 ∪ 垂直闭合（1D 核只沿单轴操作，边界恒轴向）。
4. **`build_skin` 里 glass/rail 两层按 `axis_only=False` 进外皮**：斜向玻璃剖线/异形平台栏杆把斜线一路传进包络 → 公卫西北角被切出 0.14×0.25m 斜角（表现为 60 级 0.01m 锯齿斜线）。
   → 修：这两层改 `axis_only=True`。包络 175.9→175.0 m²，公卫顶点 121→29，斜线绝迹。

### 另外两项必要修复
- **户型外门窗过滤**：PDF 画了邻近单元的门窗（8 个，x≈3.99~8.82）→ 3D 里悬在户外。
  判据：洞口中点须落在「本户型所有房间并集 `.buffer(0.45, join_style=2)`」内 → 33→17 干净剔除。**注意 buffer 必须 `join_style=2`（mitre）否则圆角破坏正交**。
- **墙裁剪到户型**：`walls_poly` 原含邻居 + 公共走道墙（13 段里 4 段悬空）。
  同上 keep_zone（buffer 0.40, mitre）求交 → 13→9 段。

### 结果（验收）
- **全输出严格正交**：房间 368 边 / 墙 377 边 / 屋顶 809 边 → **非轴向 = 0**。
- 12 房间 / 11 门（**swing 8 + sliding 3**）/ 6 窗 / 套内 **123.82 m²**。
- 三个玻璃移门：`D0` 客厅阳台 4.37m、`D5` 小孩房阳台 1.85m、**`D8` 厨房阳台 1.00m @(12.685, 4.602)** ✓
- 公卫 `R11`：1.73 m²，正交矩形 + 管道井凹角，门 `D16 swing 0.78` 贴 `[R11,R0]` ✓；北窗 `D6 window 1.26` ✓。
- 前端 `App.jsx`：新增 `GlazingUnit`（按宽度切 n 扇、相邻错开 0.035m、半透玻璃 + 下槛轨）修掉「sliding 退化成空洞」；新增 `WindowUnit`（窗台墙 0.9 + 玻璃 + 窗楣 2.4，此前 `graph.windows` 根本没被消费）；漫游初始站位改为主房间质心朝北；墙色 `#8c8680→#b3afa9`、ambient 0.25→0.5 提亮室内。
- 新增诊断工具：`pipeline/clean_render.py`（白底净化渲染，可 full/crop）、`pipeline/verify_overlay.py`（叠 PDF 原图）。
- 截图：`data/shot_top.png`（俯视）、`data/shot_walk.png`（漫游，正对玻璃移门）。
- 预览服务：`vite --port 5188`（本轮到 5188，沿用 `capture.sh` 的 5199 也行）。

### 环境经验补充
- Bash 工具 PATH 修复改用：`export PATH="/usr/bin:/bin:/c/Windows/System32:/c/Windows:$PATH"` + `export MSYS_NO_PATHCONV=1`，然后 `cd /c/Users/...`（Unix 风格），比 PortableGit 路径稳。
- 确认可用 venv：`C:/Users/super/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（triangle 20250106 / shapely / cv2 / fitz 齐全）。
- `fitz` 会打 deprecation warning，不影响。

---

## 第 8 轮（与用户对齐「我的理解」；发现镜像 bug + 主卧套间结构错）

### 用户纠正（4 条）
1. 玻璃移门扇型：厨房阳台=**一侧固定+一侧移动**；客厅/小孩房阳台=**中间两扇固定+左右两扇移动**。
2. 主卧套间内**有衣帽间**，PDF 标注「**女衣帽间**」「**男衣帽间**」（我此前完全漏了）。
3. **主卧入口门**应是走廊进主卧的**唯一入口**，现有门不符常理 → 应**镜像翻转 180°**（门轴换到另一端）。
4. 主卧套间内的衣帽间用**玻璃移门**隔断（中间固定、两侧移动）。

### 重大根因发现
- **我的平面图/3D 是「上下镜像」的**：`make_plan.py` 用 `ty(y)=MT+(ymax-y)*S` 把 y 翻了过来，而数据坐标本就是 PDF 坐标（y 向下）。→ 所有门在图上看着开向都反（左右手互换）。**修法：绘图 `ty(y)=(y-ymin)*S`，不翻。**
- **主卧套间结构错**：我原 `R1 主卧套间` 只覆盖主卧，把「女/男衣帽间」整块当墙/户外丢了。PDF 实况（页米，y 向下）：
  - 衣帽间带：y[10.95,12.22]，西边界 x≈6.85，东边界 x≈11.0；中间隔墙/柱 x[8.92,9.48]。
  - 女衣帽间 x[6.85,8.92]，男衣帽间 x[9.48,11.00]。
  - 主卧 y[12.22,16.2]；飘窗 y[16.2,16.85]；主卫 x[10.26,11.74] y[13.42,16.2]；瑶瑶衣帽间 x[11.92,…] y[13.42,16.84]。
  - 衣帽间南墙(y≈12.22)有洞口 x[7.4,8.55]（女）、x[9.44,10.45]（男）→ 即衣帽间玻璃移门。
  - 主卧入口门：页 x[10.97,11.75] y≈12.2，**门轴在东端、向南开**（我原 D11 轴在西端 → 需翻转）。

### 本地产物
- `pipeline/make_overlay.py`：把「我的房间/墙/门/窗」叠到淡化 PDF 上，**方向与 PDF 一致** → `data/v6/overlay_full.png`、`overlay_master.png`（诊断用）。
- `pipeline/make_plan2.py`：修正版平面图（补 男/女衣帽间、主卧门已翻转、移门按固定/移动扇）→ `data/v6/plan_v2_full.png`、`plan_v2_master.png`。
- PDF 整页渲染/局部裁剪：`pdf_full_2x.png`、`pdf_ms_wide.png`、`pdf_junction.png`、`pdf_ms_entry.png`、`pdf_textmap.png`（仅文字层）。

### 关键方法
- PDF 文字是矢量轮廓：`get_text` 返回 0；但 **pymupdf 直接渲染后我（多模态）能读**。大图会被工具降采样 → **裁剪尺寸控制在 ~1000px** 再 Read，避免误判比例。
- 尺寸校验：`E-dim`/`P-DIM` 图层提线量长 → 1700/3400/1500 段实测 1661/3230/1476mm，**PT_PER_M=45.36 基本正确**（误差 ~5%）。
- 门符号判别（P-DOOR）：门轴=弧心；PDF 里主卧门轴在东端。

### 待办
- 用户确认 `plan_v2`（尤其：主卧门翻转方向、男女衣帽间边界、衣帽间玻璃移门位置、主卫门位置）。
- 确认后：重建 `room-graph.json`（拆出男女衣帽间、翻 D11）/`walls_poly.json` → 重跑三角化 → 重截 3D（并核对 3D 是否也需解镜像）。
- App.jsx：玻璃移门渲染需按「固定扇/移动扇」区分（厨房 1+1；阳台 中2固定+外2移动）。

---

## 第 8 轮·续（用户审完 plan_v2，裁定 3 条并纠正衣帽间）

### 用户裁定
- **A 主卧入口门**：门轴东端、向南（主卧内）开 → **对，保持**。
- **B 男女衣帽间**：左右无所谓、**面积应相等**；**出入口方向正确**；**本质是一个整体空间**，男女只是**后期用柜体分隔**（不是两个房间、不是两道门）。
- **C**：「主卫衣帽间」说法**作废** —— 不存在主卫衣帽间，**只有主卧衣帽间**。

### 关键结构修正（本轮核心）
- 衣帽间南侧 **不是两个门洞=两道门**，而是 **一整道 3 扇玻璃移门**：
  - PDF 把中段 **x[8.55,9.44] 画得像墙**（双线 90mm），用户指出那其实是**固定扇**；
  - 左右 **x[7.40,8.55]** / **x[9.44,10.45]** 才是可移动扇 → **中固定 + 左右移动**，跨 x[7.40,10.45]（3.05m）。
- PDF 实测（页米，y 向下，**与我的数据坐标系一致**）：衣帽间南墙 y=12.22/12.312（双线≈90mm 内隔墙）；墙段 x[6.85,7.40]、[8.55,9.44]、[10.45,11.00]、[11.09,11.83]；缺口 x[7.40,8.55]、[9.44,10.45]。
- 衣帽间内部 X 交叉方框 = **衣柜柜体**（非墙）；x[8.55,9.44] 那个是男女分界柜。
- `P-DOOR` 主卧门实测：门扇 qu 四边形 x[10.917,11.719] y≈12.47（长 0.80m）。用户裁定按「轴在东端、向南开」绘制。

### 产物
- `pipeline/make_plan2.py` 升到 v3：`EXTRA_ROOMS` 合并为单个「主卧衣帽间」；新增 `CLOSET_CABINET_X`/`CLOSET_ZONE_LABELS`（柜体示意 + 女/男小字）；`CLOSET_SLIDERS` → `CLOSET_SLIDER_Y` + `CLOSET_SLIDER_PANELS`（3 扇，中固定）；新增 `closet` 放大模式。
- 图：`data/v6/plan_v3_master.png`、`plan_v3_closet.png`（衣帽间细节）、`plan_v3_full.png`。

### ⚠️ 工具坑（重要）
- **同一条消息里并发多个 Edit 会互相覆盖**：每个 Edit 基于同一份文件快照「读→改→写」，并行时后写覆盖先写 → 只有部分编辑存活（本次发 5 个只生效 2 个），**且工具仍报 success**。
  → **铁律：对同一文件的多个编辑必须串行逐个发，改完 grep 复核关键行。**

---

## 第 9 轮 · 公卫(厨房旁)真相反转 + 理解平面图 v4

### 公卫：我原来的模型是错的（用户最早就在问这个）
- **PDF 真身 = L 形**（页米，y 向下）：
  - 北段（内间）：x[10.36,11.25] y[1.83,2.62]，≈0.89×0.79m —— 里面只有"POWDER 公卫"文字 + 一个 **六芒星洁具符号**(#00B8B8, P-TOILET, 中心≈(11.05,2.25))，我判为**地漏/淋浴湿区**。
  - 南段（主间）：x[10.36,11.89] y[2.62,3.51]，≈1.53×0.89m —— 东端放**马桶**（x[11.05,11.70] y[2.87,3.33]，面朝西，水箱靠东墙 x11.7）。
  - 北段/南段之间：**0.89m 两扇推拉玻璃门**（y=2.58/2.66 双线 + 中段 x[10.72,11.08] 阶梯状错开扇 = 推拉符号）。→ 用户最早说的"最里面的空间里有一个玻璃移门"就是这道。
  - 入口：南墙 y3.51–3.63 平开门，洞口 x[10.40,11.17]，**轴在西端(x10.40)、向北开进房间**。
  - 窗：北墙 y1.61–1.66 玻璃剖线，跨 x[10.63,11.83]（D6）。
  - 东邻 x[11.45,11.92] y[1.70,2.62] = 管道井/设备（封闭）；再东 x≥11.9 是厨房操作台+管道井。
- **我的旧模型错在哪**：① 当成一个矩形 x[10.35,11.49]y[1.59,3.48]（东端马桶区被切掉、北界多算 0.24m）；② **中间被我自己加了一道假墙** y≈2.03–2.22。

### 假墙根因（管线级 bug，待修）
- `detect_gaps2()` **逐行扫描整张图**，把"同一行里两堵墙之间的空档"当门洞。公卫这一行(y≈2.125)西墙(x10.40)到东墙(x11.28)恰好空 0.88m → 被判成窄洞口(<1.2m 免覆盖率佐证) → `seal_gaps` 封成一条 0.88m 横墙 → 公卫被切成两层。
- `seal_ok` 仲裁没拦住：它只用**旧 room-graph 多边形**做点采样，旧公卫多边形是垃圾(16 顶点斜边)，探针点落在多边形外 → na=None → 直接放行。
- **修法（未实施）**：改自洽不动点迭代 —— 先封堵→标房间→"封堵条两侧最终落到同一房间"的封堵条丢弃→重封堵，迭代至稳定。（比单次旧多边形仲裁可靠）

### 门开向：改为从 PDF 开启弧自动推出
- `P-DOOR` 橙(#FF7F00) 的 curve 即开启弧。对每扇平开门：取门洞方向 u、左手垂直 n，弧点质心在 n 上的投影 s → s>0 开向 +n，否则 -n。
- 验证：公卫门 → **轴西端、向北开进房间** ✓ 与 PDF 一致。已写入 `make_plan3.py::swing_sides()`。

### 产物
- `pipeline/make_plan3.py`（= 我的理解平面图 v4，模式 full|master|gw）：公卫改 L 形、衣帽间整体+3扇移门、主卧门翻转、门开向自动推导；图例自动换行。
- 图：`data/v6/plan_v4_full.png`、`plan_v4_gw.png`（公卫细节）、`plan_v4_master.png`。
- 诊断工具：`pipeline/layer_shot.py`（按图层组渲染 PDF 局部）、`pipeline/gw_check.py`（掩膜叠 PDF 核对）。
- 坑：fitz `get_pixmap` 全页高倍会报 "Overly large image"，**必须用 `clip=` 局部渲染**。

### 待用户确认 / 未解决
- 公卫的**洗手台**没找到（南段只有马桶，北段只有星符号）——用户说"里面有洗手台、马桶"。
- 北段那间到底是淋浴间还是别的。
- 公卫内置移门扇数（我按 2 扇画）。
- 管线假墙未修；数据仍未按 v4 重建，3D 仍旧数据。

## 第 10 轮：用户 5 条答复 → 公卫真相 + v5 理解图（`make_plan5.py`）
用户答复：①洗手台在马桶下方、客厅进入公厕的靠厨房墙那边 ②北段=淋浴/湿区✓ ③内置移门 2 扇✓ ④入户门往外开 ⑤飘窗与房间连通、只有 50cm 高。

**关键发现（PDF 实测，PT=45.36）——公卫其实是「三进」**：
- 淋浴间：x10.36–11.24, y1.83–2.62（0.88×0.79）；内有花洒/地漏符号(星形, c≈(11.05,2.16))。
- 淋浴隔断：y≈2.62，**2 扇玻璃移门**（P-TOILET 图层里的双线错开扇）。
- 马桶间：x10.36–11.89, y2.62–3.51；马桶 x11.21–11.89,y2.87–3.31（椭球+水箱，水箱贴东墙 x11.89，面朝西）。
- 盥洗间(干区)：x10.40–11.89, **y3.67–4.52，南侧向客厅开敞**（南墙只在 x11.27–11.92 有；x10.40–11.27 是开口）。**洗手台就在这里面**：P-TOILET 实体中心 (11.61,4.10)、0.55×0.85矩形台面＋椭圆台盆＋龙头，**贴东墙 x11.89** —— 正好是"马桶正下方"✓。
- 公卫门：南墙 y3.51，洞口 x10.39–11.17，轴在西端 x10.39，向北开进马桶间。
- 管井：x11.24–11.92, y2.16–2.66（四面封闭，非房间）。
- 我旧模型 R11(10.35–11.49,1.59–3.48) 只覆盖了淋浴+马桶的一部分，**漏掉整个盥洗间和洗手台**。

**入户门**：洞在横墙 y≈8.57，洞口 x9.01–10.03；门扇(橙 quad) x≈9.94–9.98, y7.72–8.64；开启弧自(9.96,7.72)扫到(9.06,8.64) → **轴在东端 x≈9.96、向北(外)开**。我的 D9 原 hinge 在西端 x9.05 → 方向反，已改。

**飘窗**：主卧南侧 PDF 明确标"飘窗"（x6.9–10.9, y16.2–17.0）；R10(x11.94–14.49,y16.2–17.0) 在我这里也按飘窗，但该处 PDF 只见柜体(X框)+南窗，无"飘窗"字样 → 待用户确认。

**本轮产物**：`pipeline/make_plan5.py` → `data/v6/plan_v5_{gw,master,full}.png`。
**南带校验**：PDF 南带竖墙 x≈10.15–10.30 / 11.70–11.94 / 14.47–14.71，与我的 R6 主卫(10.28–11.72)、R5 瑶瑶衣帽间(11.94–14.47) **吻合** ✓（先前误判"偏移"是看淡底图看花了）。

### 仍未做
- 管线 `geom_v6.py` 的假墙 bug 未修（逐行扫"墙间空档"当门洞 → 公卫中间被误封）。
- 数据未按 v5 重建 → 3D 仍是旧数据；飘窗 0.5m 台面、公卫三进、入户门外开均未进数据/3D。
