# ── 依赖 / 构建产物 ──────────────────────────────────────────
app/node_modules/
app/dist/
app/.vite/

# ── 运行期临时文件 ───────────────────────────────────────────
data/_*
data/*.png

# ── 单文件演示页是构建产物，可由 tools/build_standalone.py 重生成 ──
# （当前保留入库，方便 clone 后直接双击；如需瘦身可取消下一行注释）
# demo/

# ── 大体积中间产物（可由 pipeline 一键重跑生成） ───────────────
*.npy
# 一次性诊断大图（PDF 原文裁剪 / 叠图验证），保留理解图与截图即可
data/v6/pdf_*.png
data/v6/verify_*.png
data/v6/overlay_*.png
data/v6/unit_*.png
data/v6/clean_*.png
data/v6/skin*.png
data/v6/seal*.png
# 调相机参数过程中的中间截图：只保留定稿的那几张（shot_envDefault / shot_skyTest /
# shot_topFull / shot_kitchenFix / shot_kitchenIso），其余用完即弃
data/v6/shot_orbit.png
data/v6/shot_orbitA.png
data/v6/shot_orbitB.png
data/v6/shot_envAlt.png
data/v6/shot_envHigh.png
data/v6/shot_envWide.png
data/v6/kitchen_plain.png
data/v6/kitchen_south_band.png

# ── 原始素材：只保留矢量 PDF（管线主源），其余为大文件/未使用 ──
assets/walkthrough.mp4
assets/chunxiao.dwg

# ── 系统 ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── 助手本地工作记忆（非项目交付物，不进公开仓库） ────────────
.workbuddy/
